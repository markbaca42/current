extends layout.pug
    
block content
    h1 Welcome! #{user.id} ✨
    //img(src=user.images[0].url, class='circular_image')
    .top_row 
      - var artist0_pic = artists[0].images[0].url
      - var artist0_id = artists[0].id
      - var artist1_pic = artists[1].images[0].url
      - var artist2_pic = artists[2].images[0].url
      - var artist3_pic = artists[3].images[0].url
      - var artist4_pic = artists[4].images[0].url

      img(src=artist0_pic, id= {artist0_id}, class='circular_image')
      img(src=artist1_pic, class='circular_image')
      img(src=artist2_pic, class='circular_image')
      img(src=artist3_pic, class='circular_image')
      img(src=artist4_pic, class='circular_image')
    .2nd_row 
      p #{artist0_id}
    .3rd_row
      a(class='button' href='//google.com') Google
    .4th_row 
    .bottom_row


    table 
      each t in artists 
        tr  
          - var artist_name = t.name
          - var genre = t.genres[0]

          td #{artist_name}
          td #{genre}
   
    //- table 
    //-   each t in tracks
    //-     tr 
    //-       - var artist_name = t.track.artists[0].name
    //-       - var track_name = t.track.name
    //-       - var track_preview = t.track.preview_url
    //-       - var artist_id = t.track.artists[0].id
    //-       - var track_id = t.track.id 

    //-       td #{artist_name}
    //-       td: a(href='/recommendations?artist=' + artist_id+'&track='+track_id) #{track_name}
    //-       td: a(href=track_preview) Preview